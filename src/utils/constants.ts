// Game constants — all tile dimensions and config values live here

export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;

// Isometric tile dimensions (Fallout 1 standard after FRM conversion)
export const TILE_WIDTH = 80;
export const TILE_HEIGHT = 40;
